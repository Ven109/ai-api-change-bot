node_modules/

# Throwaway migration workspaces. Everything else under .acb/ is meant to be
# committed: the manifest, the seen-changes state and the spec snapshots are
# what make later runs incremental.
.acb/work/
.acb/egress/

.DS_Store
__pycache__/
*.pyc

# Generated demo state: the fixtures regenerate this on every run.
examples/*/.acb/
